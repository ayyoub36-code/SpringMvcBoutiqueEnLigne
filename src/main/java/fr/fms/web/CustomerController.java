package fr.fms.web;

import org.springframework.stereotype.Controller;

@Controller
public class CustomerController {

	// afficher le formulaire d ajout du customer customer.html ("/customer")

	// enregistrer le customer CustomerController => saveCustomer => ibusness => impl
	// customerRepo => save(Customer);

}
